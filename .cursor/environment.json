{
  "name": "jamahlmock11/jamahlmock",
  "install": "pip install -e \".[dev]\"",
  "start": "python3 platform_run.py --web --port 8080",
  "ports": [
    {
      "name": "mispricing-dashboard",
      "port": 8080
    }
  ],
  "terminals": [
    {
      "name": "web-dashboard",
      "command": "python3 platform_run.py --web --port 8080",
      "description": "Production platform dashboard (KXBTC15M + KXBTCD) — scan only, no live orders"
    }
  ]
}
